package main

import "fmt"

func main() {
	fmt.Println("Here we should initialize an in memory data store...")
	fmt.Println("Here we should query that data store for admin accounts, then for each print the email, and a list of login user names, and ip addresses")
}
